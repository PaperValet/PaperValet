version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
    - path: plugins-external/
      linters:
        - errcheck
    - path: plugins/builtin/
      linters:
        - staticcheck
        - unused
run:
  timeout: 5m
